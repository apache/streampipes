apiVersion: v1
kind: Service
metadata:
  name: processors-statistics-flink
  labels:
    name: processors-statistics-flink
spec:
  selector:
    app: processors-statistics-flink
  ports:
    - name: main
      protocol: TCP
      port: 8090
      targetPort: 8090