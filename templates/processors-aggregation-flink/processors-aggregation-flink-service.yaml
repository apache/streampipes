apiVersion: v1
kind: Service
metadata:
  name: processors-aggregation-flink
  labels:
    name: processors-aggregation-flink
spec:
  selector:
    app: processors-aggregation-flink
  ports:
    - name: main
      protocol: TCP
      port: 8090
      targetPort: 8090