package de.fzi.cep.sepa.endpoint;

import de.fzi.cep.sepa.model.impl.graph.SepDescription;

public class SepRestlet extends AbstractRestlet<SepDescription> {

	public SepRestlet(SepDescription desc)
	{
		super(desc);
	}
		
}
