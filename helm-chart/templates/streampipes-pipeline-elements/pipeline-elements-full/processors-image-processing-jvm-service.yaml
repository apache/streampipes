{{- if (eq .Values.deployment "full") }}
apiVersion: v1
kind: Service
metadata:
  name: processors-image-processing-jvm
  labels:
    name: processors-image-processing-jvm
spec:
  selector:
    app: processors-image-processing-jvm
  ports:
    - name: main
      protocol: TCP
      port: 8090
      targetPort: 8090
{{- end }}
