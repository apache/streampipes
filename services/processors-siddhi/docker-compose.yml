version: "2"
services:
  processors-filters-siddhi:
    image: ${SP_PE_DOCKER_REGISTRY}processors-filters-siddhi:${SP_PE_VERSION}
    depends_on:
      - "consul"
#    ports:
#      - "8098:8090"
    environment:
      - SP_ICON_HOST=${SP_ICON_HOST}
    networks:
      spnet:

