version: "2.0"
services:
  mysql:
    image: mysql
    volumes:
      - mysql:/var/lib/mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_USER: user1
      MYSQL_PASSWORD: uY-F8c5)(!{H?EE}
      MYSQL_ROOT_PASSWORD: 7u*4rAy<rPh!v6@5
    networks:
      spnet:

volumes:
  mysql:

networks:
  spnet:
    external: true
