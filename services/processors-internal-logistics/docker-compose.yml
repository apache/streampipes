version: "2"
services:
  processors-internal-logistics:
    image: ${SP_INTERNAL_PE_DOCKER_REGISTRY}/processors-internal-logistics:${SP_PE_VERSION}
    depends_on:
      - "consul"
#    ports:
#      - "8098:8090"
    environment:
      - SP_ICON_HOST=${SP_ICON_HOST}
    networks:
      spnet:

