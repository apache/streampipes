version: "2.0"
services:
  postgres:
    image: postgres
    volumes:
      - ./config/postgresql/data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
       POSTGRES_USER: admin
       POSTGRES_PASSWORD: admin
    networks:
      spnet:
