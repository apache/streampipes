version: "2"
services:
  pipeline-elements-all-jvm:
    image: ${SP_PE_DOCKER_REGISTRY}pipeline-elements-all-jvm:${SP_PE_VERSION}
    depends_on:
      - "consul"
#    ports:
#      - "8098:8090"
    environment:
      - SP_ICON_HOST=${SP_ICON_HOST}
      - SP_KAFKA_HOST=${SP_KAFKA_HOST}
    networks:
      spnet:

networks:
  spnet:
    external: true
